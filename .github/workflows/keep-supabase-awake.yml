name: Keep Supabase Awake

on:
  schedule:
    # Day 1 (runs every 2 days)
    - cron: "0 0 */2 * *"     # 00:00
    - cron: "0 10 */2 * *"    # 10:00
    - cron: "0 20 */2 * *"    # 20:00

    # Day 2 (alternate days)
    - cron: "0 6 1-31/2 * *"  # 06:00
    - cron: "0 16 1-31/2 * *" # 16:00

  workflow_dispatch: {}       # Allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase Project A
        run: curl -I https://sivcpdjtgysnryvfbcvw.supabase.co/rest/v1/

     
